<div style="text-align: center;">
<h1 style="text-shadow:1px 1px 0 white,2px 2px 0 black;font-weight:bold;font-size: 35px">Personal Dictionary</h1>
<span style="font-size: 11px">どこでも書ける。どこでも見れる。<br>自分だけの単語帳が作れるサービス。</span>
<div style="margin-top: 10px">
	
<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <% if @user.errors.any? %>
   <ul>
     <% @user.errors.full_messages.each do |msg| %>
     <li><%= msg %></li>
     <% end %>
   </ul>
 <% end %>
  
  <div><%= f.label :email %>
  <%= f.email_field :email %></div>

  <div><%= f.label :password %>
  <%= f.password_field :password %></div>

  <% if devise_mapping.rememberable? -%>
  <%= f.check_box :remember_me -%> 
  <%= f.label :remember_me, :style => "font-size: 10px"-%>
  <% end %><br />
  <%= f.submit "ログイン", :class => "btn btn-primary" %>
  
<% end %>
<br />
  <span style="font-size: 10px">Twitter,Facebookで登録しても通知される事はありません。</span><br>
  <%= link_to image_tag('twitter_login.png'),"/auth/twitter" %><br><br />
  <%= link_to image_tag('face_login.png'), "/auth/facebook"%><br><br>
  
  
  
<%= render "devise/shared/links_smart_phone" %><br>
</div>
</div>
